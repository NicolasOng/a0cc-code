__pycache__/
dfs_progress.pkl
data/
graphs/
solve-data/
logs/
data/
plots/
trained_models/
app.log
a0_data/
investigations/

neighboring_states/
iteration_training_states.p
training_states.p
random_states.p
sampled_training_states.p
sampled_neighboring_states.p
test_label2.png
testing.p
training_data/
action_accuracy.png
policy_results.p

default/

output/
sweep-output/
input/
# `input/` only matches a directory; worktrees and extra checkouts symlink
# `input` at the repo root, so ignore the bare name as well.
/input

memory/
.vscode/

# stray model checkpoints dropped at the repo root (37MB each); keep them in
# output/ or input/ instead, both of which are already ignored
/*.pkl
